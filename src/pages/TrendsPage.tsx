import { AppStyled } from "appWrapp";
import { MainSection, SideBar } from "components";

export const TrendsPage = () => {
  return (
    <AppStyled>
      <SideBar />
      <MainSection />
    </AppStyled>
  );
};
